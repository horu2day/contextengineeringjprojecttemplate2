# Gemini 개발 워크스페이스: Babylon.js 가우시안 스플래팅 뷰어

> 이 문서는 Gemini CLI를 사용하여 Babylon.js 가우시안 스플래팅 뷰어를 개발하기 위한 중앙 허브입니다. 프로젝트의 목표, 기술적 제약사항, 현재 진행 상황 및 다음 작업을 정의합니다.

## 1. 프로젝트 요약 및 목표

- **핵심 목표:** Babylon.js를 사용하여 사용자가 `.splat` 및 `.ply` 파일을 로드하고, 3D 공간에서 자유롭게 탐색하며, 특정 지점의 월드 좌표를 확인할 수 있는 고성능 웹 기반 뷰어를 구현합니다.
- **주요 기능:**
  - 로컬 `.splat`, `.ply` 파일 로딩 (초기에는 에셋 폴더, 추후 드래그 앤 드롭 지원)
  - 마우스 기반 카메라 컨트롤 (회전, 줌, 패닝)
  - 3D 모델 내 특정 지점 클릭 시 월드 좌표 표시
  - 반응형 캔버스 (브라우저 창 크기 조절에 대응)
- **기술 스택:**
  - **3D 렌더링:** Babylon.js (`@babylon.js/core`, `@babylonjs/loaders`)
  - **언어:** JavaScript (ES6 Modules)
  - **개발 서버:** `http-server`
  - **패키지 매니저:** npm

## 2. Gemini를 위한 지시사항 및 페르소나

- **너의 역할:** 너는 Babylon.js와 현대 JavaScript(ESM)에 능숙한 시니어 3D 그래픽스 엔지니어다. 항상 비동기 처리, 오류 처리, 클린 코드를 염두에 두고 코드를 작성한다.
- **코딩 스타일:**
  - ES6 모듈(`import`/`export`)을 사용한다. `require()`는 사용하지 않는다.
  - 비동기 작업에는 `async/await`와 `try...catch`를 적극적으로 활용한다.
  - 변수는 `camelCase`, 클래스는 `PascalCase`를 따른다.
  - Babylon.js의 공식 문서와 베스트 프랙티스를 따른다.

## 3. 핵심 컨텍스트 및 **치명적인 주의사항 (CRITICAL)**

> **경고:** 아래 규칙을 반드시 지켜야 합니다. 위반 시 렌더링이 깨지거나 프로젝트가 동작하지 않을 수 있습니다.

1. **좌표계:** **반드시 왼손 좌표계(Babylon.js 기본값)를 사용해야 한다.** 가우시안 스플래팅은 오른손 좌표계를 지원하지 않는다. (`scene.useRightHandedSystem = true;` 코드 절대 사용 금지)
2. **비동기 로딩:** 모든 파일 로딩은 비동기(`SceneLoader.ImportMeshAsync`)로 처리해야 한다.
3. **캔버스 리사이징:** `engine.resize()`를 호출하는 `window.addEventListener("resize", ...)` 핸들러는 필수다.
4. **모듈 로더:** `.splat`, `.ply` 파일 로더를 활성화하려면 `@babylonjs/loaders` 패키지를 `import "@babylonjs/loaders";`와 같이 **side-effect로 반드시 import** 해야 한다.
5. **메모리 관리:** 페이지를 떠나기 전에 `engine.dispose()`와 `scene.dispose()`를 호출하여 메모리 누수를 방지한다.

## 4. 프로젝트 구조

```bash
babygaussianviewer/
├── src/
│   ├── main.js                # 애플리케이션 진입점 (엔진, 씬 설정, 렌더 루프)
│   ├── viewer.js              # 뷰어 핵심 로직 (씬 생성, 모델 로딩, 카메라 설정)
│   └── utils.js               # 유틸리티 함수 (좌표 피킹 등)
├── styles/
│   └── main.css               # 기본 스타일링
├── assets/
│   └── train.splat            # 예제 3D 데이터
├── index.html                 # 메인 HTML 파일
├── package.json               # 의존성 및 스크립트
└── gemini.md                  # 이 파일 (Gemini 개발 워크스페이스)

5. 개발 워크플로우 및 명령어
1. 의존성 설치 (최초 1회)
code
Bash
npm install
2. 개발 서버 실행
code
Bash
# 캐시 비활성화 모드로 서버를 시작하고 브라우저를 자동으로 엽니다.
npm run dev
3. Gemini CLI를 이용한 개발 패턴
작업 정의: 아래 6. 현재 개발 상태 및 다음 작업 섹션에서 수행할 작업을 결정합니다.
명령어 실행: Gemini CLI를 사용하여 코드 생성을 요청합니다. (예: @src/utils.js 파일에 좌표 피킹 함수를 추가해줘)
코드 검토 및 적용: 생성된 코드를 검토하고 프로젝트에 적용합니다.
테스트: 브라우저에서 기능이 올바르게 동작하는지 확인하고 콘솔 오류가 없는지 검사합니다.
문서 업데이트: 작업이 완료되면 이 gemini.md 파일의 6.과 7. 섹션을 업데이트합니다.
6. 현재 개발 상태 및 다음 작업

package.json 및 의존성 설정 완료

index.html 기본 구조 및 캔버스 생성 완료

src/main.js에서 Babylon.js 엔진 및 렌더 루프 초기화 완료

src/viewer.js에서 기본 씬, 카메라, 조명 생성 및 train.splat 모델 로딩 완료

(다음 작업) src/utils.js에 마우스 클릭 시 월드 좌표를 가져오는 기능 구현

src/main.js에서 utils.js의 좌표 피킹 기능을 씬에 연결

드래그 앤 드롭으로 파일 로드 기능 추가
➡️ 다음 목표: 사용자가 3D 모델의 특정 지점을 클릭했을 때, 해당 위치의 월드 좌표(X, Y, Z)를 브라우저 콘솔에 출력하는 기능을 구현합니다.
7. Gemini 작업 로그
이 섹션에 Gemini와의 주요 상호작용을 기록하여 작업 내역을 추적합니다.
작업 1: 월드 좌표 피킹 기능 구현
[Prompt 예시]
code
Code
@src/utils.js 파일을 생성하고, `setupCoordinatePicking`이라는 이름의 함수를 만들어줘. 이 함수는 scene과 canvas를 인자로 받고, canvas에서 'click' 이벤트가 발생했을 때 scene.pick을 사용해서 클릭된 지점의 월드 좌표를 찾아 콘솔에 "World Coordinates: { x: ..., y: ..., z: ... }" 형식으로 출력해야 해. 이 함수를 export 해줘.
[Gemini 응답 요약/코드]
code
JavaScript
// Gemini가 생성한 코드를 여기에 붙여넣기
[검토 및 수정 사항]
...
code
Code
### 이 수정안의 핵심 개선점

1.  **명확한 역할 부여 (2번 섹션):** Gemini에게 '시니어 3D 그래픽스 엔지니어'라는 구체적인 페르소나를 부여하여 결과물의 품질과 일관성을 높입니다.
2.  **치명적인 주의사항 강조 (3번 섹션):** `gaussian-splatting-viewer.md`에 흩어져 있던 **핵심 제약사항**들을 한 곳에 모아 Gemini가 실수를 저지를 확률을 대폭 줄입니다. 이는 매우 중요합니다.
3.  **'살아있는' 워크스페이스로 전환 (6, 7번 섹션):**
    *   **체크리스트 기반 진행 상황 관리:** 완료된 작업과 앞으로 할 작업을 명확히 구분하여 프로젝트의 현재 상태를 한눈에 파악할 수 있습니다.
    *   **명확한 다음 목표 제시:** Gemini에게 다음에 무엇을 해야 할지 구체적으로 지시합니다.
    *   **작업 로그:** 프롬프트와 결과를 기록하여 개발 과정을 추적하고, 문제가 발생했을 때 원인을 찾기 쉽게 만듭니다.

이 새로운 `gemini.md` 파일을 프로젝트의 루트에 저장하고, 앞으로 개발을 진행할 때마다 **이 파일을 먼저 열고 업데이트하면서** Gemini CLI를 사용하시면 훨씬 더 체계적이고 효율적인 개발이 가능할 것입니다.
```
